# Dependencies
node_modules

# Raycast build output
dist

# Generated by Raycast on `ray develop` / API version bumps — do not commit (see Store docs)
raycast-env.d.ts

# Local env / secrets — never commit API keys
.env
.env.*

# Caches / logs
.eslintcache
*.log

# macOS
.DS_Store
